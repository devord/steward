{
  "name": "@devord/steward",
  "version": "0.3.0",
  "description": "Keep your Steward dashboard's routines in sync — cloud routines and local launchd agents from one routines.yaml.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/devord/steward.git",
    "directory": "packages/cli"
  },
  "bin": {
    "steward": "./dist/cli.js"
  },
  "files": [
    "dist",
    "skills"
  ],
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "node build.mjs",
    "test": "vp test --run",
    "typecheck": "tsc"
  },
  "devDependencies": {
    "@steward/schema": "workspace:*",
    "@types/node": "catalog:",
    "esbuild": "catalog:",
    "vitest": "catalog:",
    "zod": "catalog:"
  },
  "engines": {
    "node": ">=20.11"
  }
}
