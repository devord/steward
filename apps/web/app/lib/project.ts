/**
 * Facts about Steward itself — as opposed to the user's data repos, which
 * `repos.ts` addresses. Steward is open source, and its own repo is a
 * destination the chrome links out to (the landing corner and footer, the
 * account menu, the docs nav), so the URL is named once here rather than
 * spelled out at each of them.
 */

/** Steward's source repo. */
export const STEWARD_REPO_URL = "https://github.com/devord/steward"
