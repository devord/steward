{
  "name": "bulletin",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "vp check",
    "test": "turbo run test",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck && tsc -p scripts",
    "dev": "pnpm --filter @bulletin/web dev",
    "routine": "node scripts/routine.ts",
    "routine:trigger": "node scripts/routine-trigger.ts",
    "routines:sync": "node scripts/routines-sync.ts"
  },
  "devDependencies": {
    "@bulletin/schema": "workspace:*",
    "@types/node": "catalog:",
    "lefthook": "^1.13.0",
    "turbo": "^2.10.4",
    "typescript": "^7.0.2",
    "vite-plus": "^0.2.4",
    "yaml": "catalog:",
    "zod": "catalog:"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@10.17.1"
}
