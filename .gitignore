.DS_Store
node_modules
dist
build
# Built into the CLI package by build.mjs (copied from .claude/skills).
packages/cli/skills/
.turbo
.react-router
# fumadocs-mdx build output (generated from apps/web/content/docs)
.source
.vercel
coverage
.env
.env.local
*.local.json
.humanlayer/tasks/

# Personal impeccable skill copy (installed by npx impeccable update)
.claude/skills/impeccable/
# Its critique snapshots — local backlog for the skill's own commands, and
# markdown the formatter would otherwise claim as repo content.
.impeccable/

# Transient harness worktree copies of the repo
.claude/worktrees/
design-shots/
.env*

# Vitest browser-mode artifacts (failure screenshots, attachments)
__screenshots__/
.vitest-attachments/
